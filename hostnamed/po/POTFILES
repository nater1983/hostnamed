# Files containing translatable strings.
# Paths are relative to the project root.
# Run 'ninja -C build hostnamed-pot' to regenerate hostnamed.pot after editing.

src/main.c
src/rcl-hostname.c
data/org.freedesktop.hostname1.policy
